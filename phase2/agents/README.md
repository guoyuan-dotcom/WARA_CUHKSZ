# Compatibility Package

The canonical WARA agent interface is `wara_core/agents`.

This package is retained for compatibility with existing Phase 2 scripts. New
code should import role agents and registry helpers from `wara_core.agents`.
